import { Box } from '@mui/material';
import { Calendar } from '../../components/Calendar';

const CalendarPage = () => {
  return (
    <Box p={2}>
      <Calendar />
    </Box>
  );
};

export default CalendarPage;
